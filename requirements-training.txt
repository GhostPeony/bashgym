# Compatibility wrapper for older local/private training instructions.
# This installs BashGym plus the provider-neutral training stack declared in
# pyproject.toml. NeMo RL/Gym remains a separately pinned optional runtime.
-e .[training]

# Optional backend-specific accelerators are intentionally operator-selected:
# liger-kernel>=0.5.0
# unsloth
