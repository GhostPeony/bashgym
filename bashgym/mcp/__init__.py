"""BashGym MCP servers — expose the agent sandbox as Model Context Protocol tools.

Used by NeMo Data Designer (v0.6.x) tool-use columns to generate *real* tool-use
trajectories during synthetic data generation, rather than simulated ones.
"""
